/**
 * Configuração dinâmica da API
 * Este arquivo é servido pelo servidor (Apache/Node.js)
 * Não é bundled pelo Vite, portanto pode ser alterado sem rebuild
 * 
 * Em produção, este arquivo é lido do servidor:
 * /var/www/html/config.js (Apache EC2)
 */

window.__APP_CONFIG__ = {
  API_URL: 'https://api.solo-forte.tech/api/products'
}
